import Home from "./pages/Home";

/**
 * Root application component. Currently renders the Home page directly;
 * once React Router is wired up, this will instead render a set of routes.
 */
function App() {
  return <Home />;
}

export default App;
